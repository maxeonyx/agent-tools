name: Unsafe ledger writer

on: pull_request

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: cargo test
      - run: |
          git add .test-status.json
          git commit -m "update ledger"
          git push --force
